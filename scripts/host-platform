#!/usr/bin/env bash
set -euo pipefail

case "$(uname -m)" in
  arm64 | aarch64)
    printf 'linux/arm64\n'
    ;;
  x86_64 | amd64)
    printf 'linux/amd64\n'
    ;;
  *)
    echo "unsupported host architecture: $(uname -m)" >&2
    exit 65
    ;;
esac
